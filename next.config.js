/** @type {import('next').NextConfig} */
const nextConfig = {
  // API routes are needed for the contact form (Resend email)
  // Deploy on Render as a Web Service (Starter $7/mo), NOT a Static Site
};

module.exports = nextConfig;
